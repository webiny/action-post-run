name: "Action Post"
description: "Enables executing custom commands once a workflow job has ended."

inputs:
  command:
    description: "A commands that needs to be run."
    required: true
    default: echo "This is a post-action command..."

runs:
  using: node12
  main: dist/main/index.js
  post: dist/post/index.js
